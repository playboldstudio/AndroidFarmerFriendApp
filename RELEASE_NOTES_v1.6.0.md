# Farmer Friend v1.6.0

• Meaningful UI states — dedicated **Offline** (you're offline / retry) and **Slow network** ("still loading…" hint) screens on Market, replacing the generic error/blank load
• Update prompt — in-app update shown as a **centered dialog** in English with an explicit **Update** and **Close** action (not a top-of-screen card)
• Notifications — the full-color **app logo** now appears via the large icon on every notification
• Google Sign-In — registered the actual Play App Signing certificate so sign-in works on downloaded builds; clearer message when no Google account is available
• Logout fix — the home greeting now clears the signed-out user's name correctly (no stale name after logout)
• Profile validation — name capped at 30 characters with a live counter; mobile number hardened (+91 handled correctly, proper error messages)
• App-logo notifications, scroll-aware FAB, adaptive tablet layout, and Compose performance improvements carried forward
• 11 Indian languages supported
• Bug fixes and performance improvements
